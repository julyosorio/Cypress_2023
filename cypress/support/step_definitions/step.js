import { When, Then } from "@badeball/cypress-cucumber-preprocessor"


When('el usuario ingresa los datos correctos al Formulario', ()=>{
 desc
});

Then('el usuario ve en pantalla el mensaje de envio exitoso', ()=>{
  cy.log('paso 2')

});